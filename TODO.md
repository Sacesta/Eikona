# TODO: Update Footer.tsx

- [ ] Update imports in Footer.tsx to use lucide-react icons instead of react-icons/ci
- [ ] Replace the component definition with the new LandingPage code, renaming the component to Footer
- [ ] Ensure the component is exported as default Footer
- [ ] Verify the updated Footer.tsx renders correctly
